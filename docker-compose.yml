version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: products-api:latest
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - app-net

networks:
  app-net: {}
